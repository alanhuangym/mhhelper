{
  "name": "mhhelper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "gen-cert": "mkdir -p certs && openssl req -x509 -newkey rsa:2048 -keyout certs/server.key -out certs/server.crt -days 365 -nodes -subj '/CN=mhhelper'"
  },
  "dependencies": {
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1"
  }
}
